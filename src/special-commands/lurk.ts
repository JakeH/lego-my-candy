import { CommandContext } from '../commands/commands.model';

export function lurk(context: CommandContext) {

    // only chat, skip the queue, might need a special message.
    // retain the others in lurk mode??? Do we invalidate lurk mode
    // after the next message??

}
